;;; Web DB Viewer configuration file

[basic]
;;; Database type
; dbtype=mysql
; dbtype=sqlite
dbtype=sqlite

;;; Database name
dbname=<%= app->home->rel_file('t/test.db') %>

;;; User
user=kimoto

;;; Password
password=a

;;; Host
;host=yourhost.com
host=some.com

;;; Port
;port=1234
port=10000

;;; Site title
site_title=Web DB Viewer

[mysql]
;;; Config file
mysql_read_default_file=/etc/my.cnf

;;; SET NAMES statement
set_names=ujis

[join]
;;; Join cluase
;;; If join option is set, you can use join mode.
book[1]=left join author on book.author_id = author.id
book[2]=left join title on book.title_id = title.id

[hypnotoad]
listen=http://*:10030
